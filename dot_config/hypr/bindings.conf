# Application bindings
bindd = SUPER, RETURN, Terminal, exec, uwsm-app -- xdg-terminal-exec --dir="$(omarchy-cmd-terminal-cwd)"
bindd = SUPER SHIFT, E, File manager (CLI - spf), exec, $terminal -e spf
bindd = SUPER SHIFT ALT, E, File manager, exec, uwsm-app -- nautilus --new-window
bindd = SUPER SHIFT, B, Browser, exec, omarchy-launch-browser
bindd = SUPER SHIFT ALT, B, Browser (private), exec, omarchy-launch-browser --private
# bindd = SUPER SHIFT, M, Music, exec, omarchy-launch-or-focus spotify
bindd = SUPER SHIFT, M, YouTube Music, exec, omarchy-launch-webapp "https://music.youtube.com/"
# bindd = SUPER SHIFT, N, Editor, exec, omarchy-launch-editor
bindd = SUPER SHIFT, T, Activity, exec, omarchy-launch-tui btop
bindd = SUPER SHIFT, D, Docker, exec, omarchy-launch-tui lazydocker
# bindd = SUPER SHIFT, G, Signal, exec, omarchy-launch-or-focus ^signal$ "uwsm-app -- signal-desktop"
# bindd = SUPER SHIFT, O, Obsidian, exec, omarchy-launch-or-focus ^obsidian$ "uwsm-app -- obsidian -disable-gpu --enable-wayland-ime"
bindd = SUPER SHIFT, W, Typora, exec, uwsm-app -- typora --enable-wayland-ime
bindd = SUPER SHIFT, SLASH, Passwords, exec, uwsm-app -- 1password

# If your web app url contains #, type it as ## to prevent hyprland treating it as a comment
bindd = SUPER SHIFT, A, ChatGPT, exec, omarchy-launch-webapp "https://t3.chat"
bindd = SUPER SHIFT, C, Calendar, exec, omarchy-launch-webapp "https://calendar.google.com/calendar/u/1/r?pli=1"
bindd = SUPER SHIFT, Y, YouTube, exec, omarchy-launch-webapp "https://youtube.com/"
bindd = SUPER SHIFT ALT, G, WhatsApp, exec, omarchy-launch-or-focus-webapp WhatsApp "https://web.whatsapp.com/"
# bindd = SUPER SHIFT CTRL, G, Google Messages, exec, omarchy-launch-or-focus-webapp "Google Messages" "https://messages.google.com/web/conversations"
# bindd = SUPER SHIFT, P, Google Photos, exec, omarchy-launch-or-focus-webapp "Google Photos" "https://photos.google.com/"
bindd = SUPER SHIFT, X, X, exec, omarchy-launch-webapp "https://x.com/"
bindd = SUPER SHIFT ALT, X, X Post, exec, omarchy-launch-webapp "https://x.com/compose/post"

# Overwrite existing bindings, like putting Omarchy Menu on Super + Space
# unbind = SUPER, SPACE
# bindd = SUPER, SPACE, Omarchy menu, exec, omarchy-menu
bindd = SUPER_CTRL_ALT, R, Reload Hyprland config, exec, hyprctl reload && notify-send -a Hyprland -i dialog-information "Config reloaded" "Hyprland configuration has been refreshed."
bindd = SUPER_CTRL_ALT, W, Force kill active window, killactive

bindd = SUPER_ALT, T, Layout group toggle, togglegroup
# bindd = SUPER_CTRL L, Lock screen, exec, loginctl lock-session
bindd = SUPER_ALT, K, Show key bindings, exec, omarchy-menu-keybindings

# # Unbind conflicting defaults
unbind = SUPER, J
unbind = SUPER, P
unbind = SUPER, K

# Vim-style focus
bindd = SUPER, H, Move focus left,  movefocus, l
bindd = SUPER, L, Move focus right, movefocus, r
bindd = SUPER, K, Move focus up,    movefocus, u
bindd = SUPER, J, Move focus down,  movefocus, d

# Unbind conflicting defaults
unbind = SUPER, TAB
unbind = SUPER_SHIFT, TAB
# Better bindings
bindd = SUPER, TAB, Cycle next tab, changegroupactive, f
bindd = SUPER_SHIFT, TAB, Cycle prev tab, changegroupactive, b
bindd = SUPER_ALT, p, Password Manager, exec, hyprctl dispatch exec "[float;center;size 500 400] $terminal -e ~/.labs/scripts/utils/pass-cp.sh"

bind = SUPER_ALT, J, togglesplit, Toggle window split
bind = SUPER_ALT, P, pseudo, Pseudo window

# Custom volume controls with Super + F-keys
bindld = SUPER, F1, Mute, exec, swayosd-client --output-volume mute-toggle
bindld = SUPER, F2, Volume down, exec, swayosd-client --output-volume lower
bindld = SUPER, F3, Volume up, exec, swayosd-client --output-volume raise

# Swap fullscreen bindings - Super+F for full width, Super+Alt+F for full screen
unbind = SUPER, F
unbind = SUPER_ALT, F
bindd = SUPER, F, Full width, fullscreen, 1
bindd = SUPER_ALT, F, Full screen, fullscreen, 0
